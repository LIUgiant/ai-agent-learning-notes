document.querySelectorAll('[role=tablist]').forEach(root=>{const tabs=[...root.querySelectorAll('[role=tab]')];const select=tab=>tabs.forEach(t=>{const active=t===tab;t.setAttribute('aria-selected',String(active));t.tabIndex=active?0:-1;const panel=document.getElementById(t.dataset.target);if(panel)panel.hidden=!active;});tabs.forEach((tab,i)=>{tab.addEventListener('click',()=>select(tab));tab.addEventListener('keydown',e=>{if(['ArrowRight','ArrowLeft','Home','End'].includes(e.key)){e.preventDefault();const n=e.key==='Home'?0:e.key==='End'?tabs.length-1:(i+(e.key==='ArrowRight'?1:-1)+tabs.length)%tabs.length;select(tabs[n]);tabs[n].focus();}});});});
document.querySelectorAll('.flow-switch button').forEach(button=>button.addEventListener('click',()=>{const root=button.closest('.flow-explorer');root.querySelectorAll('.flow-switch button').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));root.querySelectorAll('.flow-panel').forEach(p=>p.hidden=p.dataset.flowPanel!==button.dataset.flow);}));
